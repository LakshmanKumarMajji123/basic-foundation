/**let var, const
 * let means simply "later". u can update the new value to the previously declared variable.
 * cosnt means fixed u cannot assign a value to the variable once it declared
 * 
 * var means before using let and const used many of them "var"
 * major difference b/w var & let & const is... var doesn't have any scopes like "global and local"
 * u can access any where in the program 
 */

 var myName = "Lakshman";
 console.log(myName);
